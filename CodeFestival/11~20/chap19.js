function solution(a, b) {
    let result = 1;
    for (let i = 1; i <= b; i++) {
        result *= a;
    }
}

//해설 답안
// const num = ['2', '6'];
// console.log(Math.pow(parseInt(num[0], 10), parseInt(num[1], 10)));
